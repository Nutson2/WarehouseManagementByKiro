@page "{id:int}"
@model WarehouseManagement.Client.Pages.Clients.EditModel
@{
    ViewData["Title"] = "Редактировать клиента";
}

<div class="row">
    <div class="col-md-6">
        <h2>Редактировать клиента</h2>
        
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            <input asp-for="Id" type="hidden" />
            
            <div class="mb-3">
                <label asp-for="Client.Name" class="form-label">Наименование</label>
                <input asp-for="Client.Name" class="form-control" />
                <span asp-validation-for="Client.Name" class="text-danger"></span>
            </div>
            
            <div class="mb-3">
                <label asp-for="Client.Address" class="form-label">Адрес</label>
                <textarea asp-for="Client.Address" class="form-control" rows="3"></textarea>
                <span asp-validation-for="Client.Address" class="text-danger"></span>
            </div>
            
            <div class="mb-3">
                <button type="submit" class="btn btn-primary">Сохранить</button>
                <a asp-page="./Index" class="btn btn-secondary">Отмена</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}